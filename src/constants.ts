import Vector from './vector';

const constants = {
  ticksPerSecond: 60,
  thrust: 60,
  maxVelocityMagnitude: 400,
  shipTurn: 10,
  maxShipRpm: 40,
  bulletSpeed: 500,
  bulletRadius: 5,
  minAsteroidSpeed: 50,
  maxAsteroidSpeed: 200,
  minAsteroidRpm: -40,
  maxAsteroidRpm: 40,
  maxAsteroidCount: 5,
};

export default constants;
